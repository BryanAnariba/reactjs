export const ProcesoPesado = (iteraciones) => {
        for (let i=0; i<iteraciones; i++) {
            console.log('Hay Vamos con la iteraciones');
        }

        return `Iteraciones realizadas`;
    } 